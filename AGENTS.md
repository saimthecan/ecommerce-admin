# AGENTS.md

Before you start:
- Read `docs/PROJECT_CONTEXT.md` and `docs/ROADMAP.md`.

Run commands
- Backend: `cd backend && python -m uvicorn app.main:app --reload`
- Frontend: `cd frontend && npm run dev`

Development rules
- Keep diffs small and focused.
- Run lint before opening a PR or sharing changes.
- Do not commit or paste secrets/env values.
